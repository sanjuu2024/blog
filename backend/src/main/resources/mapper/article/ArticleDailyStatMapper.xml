<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.ccsanjuu.blog.modules.article.mapper.ArticleDailyStatMapper">
    <insert id="incrementViewCount">
        INSERT INTO blog_article_daily_stat (article_id, stat_date, view_count)
        VALUES (#{articleId}, #{statDate}, 1)
        ON CONFLICT (article_id, stat_date)
        DO UPDATE SET view_count = blog_article_daily_stat.view_count + 1
    </insert>
</mapper>
