# 个人技术博客系统 PRD

## 1. 文档信息

- 项目名称：个人技术博客系统
- 文档版本：v1.0
- 文档日期：2026-04-22
- 文档状态：初稿

## 2. 产品概述

### 2.1 产品背景

本项目用于个人技术文章沉淀，同时作为 `Vue + Spring Boot + PostgreSQL + Redis` 的全链路练手项目。项目目标不是一次性构建复杂社区，而是在可控范围内完成从前台展示、注册登录、权限控制到后台管理的完整闭环。

### 2.2 产品目标

- 支撑个人技术文章的长期沉淀与展示
- 提供极简个人主页风格的博客体验
- 打通用户注册、登录、鉴权、文章管理、公开浏览的主链路
- 为后续评论、点赞、收藏、邮件验证等能力预留扩展空间

### 2.3 产品定位

- 类型：个人技术博客
- 风格：极简个人主页
- 核心特点：轻量、完整、可扩展

### 2.4 设计原则

- 先完成核心主链路，再逐步增加互动能力
- 首版尽量避免复杂社区化设计
- 数据库设计需为后续版本预留扩展字段和表结构
- 权限边界清晰，管理员、用户、游客职责明确

## 3. 角色定义

| 角色 | 定义 | 权限边界 |
| --- | --- | --- |
| 管理员 | 站点拥有者，负责站点运营和内容管理 | 可管理用户、文章、分类、标签，可发布和下线文章 |
| 用户 | 已注册并登录的普通用户 | 可注册、登录、维护个人资料；后续版本可参与互动 |
| 游客 | 未登录访客 | 仅可浏览公开内容 |

## 4. 版本范围与优先级

### 4.1 P0 / V1 必做范围

- 用户注册
- 用户登录
- 用户退出登录
- 双 Token 认证机制
- Refresh Token 存储与失效控制
- 用户个人中心
- 用户资料修改
- 管理员用户管理
- 管理员文章管理
- 分类管理
- 标签管理
- 文章草稿保存
- 文章发布
- 文章下线
- 文章置顶
- 游客浏览公开文章
- 分类浏览
- 基础权限控制

### 4.2 P1 后续迭代

- 评论功能
- 关于我页面
- 项目作品页面
- 留言页面
- 图片上传能力，包含用户头像、文章封面和文章正文图片
- 文章搜索
- Redis + tokenVersion 令牌版本控制，使修改密码、禁用用户、修改角色后的旧 Access Token 立即失效
- 后台管理操作审计日志，记录操作者、目标资源、操作类型、操作结果和操作时间

### 4.3 P2 后续迭代

- 点赞功能
- 收藏功能
- 友链管理
- SMTP 邮箱验证
- 文章详情 URL 可读化与 SEO 优化，例如在 URL 中追加 `slug`
- 更多统计与运营能力

## 5. 信息架构

### 5.1 前台导航

- 首页
- 文章
- 分类
- 项目
- 关于我
- 留言

说明：

- `标签` 不进入前台主导航
- 标签仅作为文章检索、筛选和后续搜索维度使用

### 5.2 后台导航

- 用户管理
- 文章管理
- 分类管理
- 标签管理

## 6. 功能范围说明

### 6.1 已确认规则

- 注册方式：用户名 + 邮箱 + 密码
- 登录方式：用户名或邮箱 + 密码
- 文章编辑器：Markdown + 第三方库
- 文章状态：草稿 / 已发布 / 已下线
- 分类采用两级结构：一级分类如 `技术`、`学业`、`消遣`、`杂记`，后续可继续新增；二级分类如 `算法`、`Java`、`前端三剑客`
- 文章必须归属一个二级分类，一级分类通过二级分类的父分类进行归属
- 仅管理员可发文
- 登录用户在 P0 仅比游客多出个人中心与资料修改能力
- 评论、点赞、收藏不在 P0 实现，但需在数据层预留
- 被禁用用户不可登录
- 已登录用户若被禁用，P0 阶段已有 Access Token 最长保留到短有效期自然过期，但 Refresh Token 刷新必须失败
- 修改密码或用户被禁用后，P0 阶段撤销该用户全部活跃 Refresh Token；修改角色不撤销 Refresh Token，新的角色在刷新登录态或重新登录后生效；P1 阶段通过 tokenVersion 让旧 Access Token 立即失效
- 公开用户资料、文章作者信息可以返回用户 ID；公开资料卡以 `userId` 作为路径标识，`username` 作为展示和登录标识

### 6.2 上传能力说明

- P0 不做图片上传服务
- P0 用户头像使用默认头像
- P0 文章封面字段保留，但不提供本地上传能力，后台可先手工录入图片 URL
- P0 文章正文可通过 Markdown 图片语法引用已有图片 URL，但不提供编辑器内上传能力
- P1 增加头像、文章封面和文章正文图片上传能力，文章内容中只保存图片 URL，不保存图片二进制

## 7. 管理员视角 PRD

### 7.1 用户管理

#### 7.1.1 目标

管理员可以对站内用户进行统一管理，确保账号状态、权限和安全可控。

#### 7.1.2 功能需求

- 查看用户列表
- 按用户名、邮箱、状态、角色筛选用户
- P0 阶段用户名和邮箱可通过一个关键词入口合并搜索；P1 阶段必须拆分为用户名、邮箱两个独立筛选项，避免后台管理时搜索条件含义不明确
- 查看用户注册时间、最近登录时间
- 用户列表默认按注册时间倒序展示，同一注册时间下按用户 ID 倒序展示
- 启用用户
- 禁用用户
- 修改用户角色

#### 7.1.3 业务规则

- 被禁用用户不能通过用户名或邮箱登录
- 管理员不能修改当前登录用户自身的状态和角色
- 禁用用户后，后端需撤销该用户全部活跃 Refresh Token；启用用户不需要撤销 Refresh Token
- 修改用户角色后不撤销 Refresh Token；新的角色在刷新登录态或重新登录后生效
- 已登录用户若被禁用或角色被变更，P0 阶段已有 Access Token 最长保留到短有效期自然过期，P1 阶段通过 tokenVersion 让旧 Access Token 立即失效
- 管理员可将普通用户设置为管理员，但首版默认仅一个管理员使用场景

### 7.2 文章管理

#### 7.2.1 目标

管理员可以完成技术文章从创建到发布、下线的完整生命周期管理。

#### 7.2.2 功能需求

- 新建文章
- 编辑文章
- 删除文章
- 保存草稿
- 发布文章
- 下线文章
- 设置文章二级分类
- 设置文章标签
- 设置文章摘要
- 设置文章封面字段
- 设置文章是否置顶
- 查看发布时间与更新时间

#### 7.2.3 文章编辑字段

- 标题
- 摘要
- Markdown 正文
  说明：系统基于 Markdown 自动生成 HTML 正文与纯文本正文
- 二级分类
- 标签
- 封面
- 置顶状态
- 文章状态

#### 7.2.4 业务规则

- 草稿文章前台不可见
- 已发布文章可在前台展示
- 已下线文章前台不可见
- `DRAFT` 表示从未发布过的草稿；`OFFLINE` 表示曾经发布过但当前下线，不应把已发布过的文章重新转为草稿
- 创建文章时仅允许保存为 `DRAFT` 或 `PUBLISHED`，不允许直接创建为 `OFFLINE`
- 从未发布过的文章可在编辑页继续保存为 `DRAFT`，也可发布为 `PUBLISHED`，不允许转为 `OFFLINE`
- 已发布过的文章仅允许在 `PUBLISHED` 与 `OFFLINE` 之间流转，不允许回退为 `DRAFT`
- `publishedAt` 表示首次发布时间，仅在文章第一次变为 `PUBLISHED` 时写入；下线、重新发布或更新已发布文章时不刷新
- 文章发布前必须绑定一个二级分类
- 管理端以 Markdown 作为文章编辑源
- 后端需根据 Markdown 自动转译并存储 HTML 正文，供前台渲染使用
- 后端需根据 Markdown 自动提取并存储纯文本正文，供后续全文搜索使用
- 置顶文章在列表中排在普通文章之前
- 多篇置顶文章之间按发布时间倒序排列

### 7.3 分类管理

#### 7.3.1 功能需求

- 新增一级分类
- 新增二级分类
- 编辑分类
- 删除分类
- 设置父级分类
- 设置分类层级
- 设置分类描述
- 设置分类排序

#### 7.3.2 业务规则

- 分类采用两级结构
- 一级分类用于内容聚合与前台导航分组
- 二级分类用于文章实际归属
- 一级分类初始默认包含：`技术`、`学业`、`消遣`、`杂记`
- 一级分类后续允许继续新增
- 二级分类必须挂载在某个一级分类下
- 文章只能绑定二级分类，不能直接绑定一级分类

### 7.4 标签管理

#### 7.4.1 功能需求

- 新增标签
- 编辑标签
- 删除标签

#### 7.4.2 业务规则

- 标签不出现在前台主导航
- 标签用于后台配置、文章关联和后续搜索筛选

### 7.5 后台权限控制

#### 7.5.1 功能需求

- 后台页面仅管理员可访问
- 后台接口必须校验管理员角色
- 无权限用户访问后台时应被拒绝并跳转登录或返回无权限提示

### 7.6 评论管理

#### 7.6.1 功能需求

- 新增后台评论管理页面，仅管理员可访问
- 管理员可按文章、评论用户、评论状态和创建时间筛选评论
- 管理员可查看待审核、已通过、已拒绝、已隐藏和已删除评论
- 管理员可通过或拒绝待审核评论
- 管理员可隐藏已经公开的评论
- 管理员可删除评论
- 审核、拒绝、隐藏和删除操作需记录操作者、操作时间及可选的处理原因

#### 7.6.2 业务规则

- `PENDING` 表示等待站长审核，`APPROVED` 表示审核通过并可公开展示
- `REJECTED` 表示审核未通过且从未公开，`HIDDEN` 表示曾公开但被管理员隐藏，`DELETED` 表示已逻辑删除
- 管理员不得修改用户发表的评论内容，只能审核、拒绝、隐藏或删除
- 删除评论时，其所有层级的子回复必须在同一事务中一并逻辑删除
- 删除评论子树时，需从文章 `comment_count` 中扣除该子树原有的全部 `APPROVED` 评论数
- 评论状态变化与文章 `comment_count` 更新必须在同一事务中完成
- P1 后台管理操作审计日志需覆盖评论审核、拒绝、隐藏和删除操作

## 8. 用户视角 PRD

### 8.1 注册

#### 8.1.1 功能需求

- 用户可通过注册页创建账号
- 注册时需填写用户名、邮箱、密码

#### 8.1.2 校验规则

- 用户名不能为空
- 用户名长度为 4-20 个字符，只允许中文、英文、数字、下划线和短横线
- 邮箱格式必须合法；注册邮箱仅支持常见邮箱格式，不能包含空格或中文字符
- 用户名唯一性按大小写不敏感处理，但保留注册时输入的原始大小写用于展示。例如允许用户注册展示名 `Sanjuu`，但不允许另一个人再注册 `sanjuu`
- 用户名注册后不可修改
- 邮箱唯一性按大小写不敏感处理；产品层统一将 `A@example.com` 和 `a@example.com` 视为同一个邮箱账号
- 密码长度为 6-20 个字符；允许中文、英文、数字、下划线、短横线和常用 ASCII 特殊字符；不允许空格等空白字符
- 注册时无需填写昵称，服务端默认使用用户名初始化昵称

### 8.2 登录

#### 8.2.1 功能需求

- 用户可通过用户名 + 密码登录
- 用户可通过邮箱 + 密码登录
- 登录成功后获取 Access Token；Refresh Token 由后端写入 HttpOnly Cookie
- 登录账号必须符合用户名或邮箱格式；包含 `@` 时按邮箱登录，否则按用户名登录
- 用户名和邮箱登录均按大小写不敏感处理。也就是允许用户注册展示名 `Sanjuu`，但不允许另一个人再注册 `sanjuu`；登录时输入 `sanjuu`、`SANJUU`、`Sanjuu` 都能找到同一个账号。邮箱同理，`A@example.com` 和 `a@example.com` 会识别为同一个邮箱账号

#### 8.2.2 业务规则

- Access Token 用于接口访问
- Access Token 有效期较短，P0 默认为 15 分钟
- Refresh Token 用于续期登录态和退出登录，由后端通过 `Set-Cookie` 写入 `refresh_token` HttpOnly Cookie，前端 JavaScript 不读取或持久化 Refresh Token
- `refresh_token` Cookie 路径为 `/api/v1/auth`，只随 `/api/v1/auth/**` 请求发送；开发和生产均按同域请求设计，不依赖 CORS
- P0 阶段 Refresh Token 以数据库会话表做存储与失效控制
- P1 阶段引入 Redis 存储运行态会话与 tokenVersion，用于降低鉴权查询成本，并支持权限变化后的旧 Access Token 立即失效
- Access Token 携带登录或刷新时的用户角色、状态快照；P0 阶段角色或状态变化后通过撤销 Refresh Token 阻止继续续期，旧 Access Token 依赖短有效期自然过期
- Refresh Token 使用轮转机制，每次刷新成功后旧 Refresh Token 立即失效，并签发新的 Refresh Token
- P0 阶段 Refresh Token 缺失、格式错误、签名无效、已过期、已撤销或找不到对应会话时，统一视为刷新失败，前端清理本地登录态并引导重新登录
- P0 阶段不因普通刷新失败自动撤销该用户全部活跃 Refresh Token；修改密码、用户禁用、管理员强制下线等明确安全事件可撤销全部会话
- P1 阶段可为刚轮转的旧 Refresh Token 设置短暂宽限期，用于同一次刷新请求的幂等重试；也可结合 Redis 与 tokenVersion 增强重放检测
- 用户退出登录时，Refresh Token 需失效
- 退出登录接口不要求有效 Access Token，后端通过 `refresh_token` Cookie 尝试撤销当前会话；若 Refresh Token 缺失、已过期、已撤销或找不到对应会话，仍视为退出成功，前端清理本地登录态即可

### 8.3 个人中心

#### 8.3.1 功能需求

- 查看个人基础资料
- 修改昵称
- 修改个人简介
- 修改密码
- 昵称长度为 1-20 个字符，不能全为空白字符
- 个人简介最长 500 个字符

#### 8.3.2 暂不支持

- 邮箱修改
- 邮箱验证
- 自定义头像上传
- 用户名修改，用户名注册后不可修改

### 8.4 内容浏览

#### 8.4.1 功能需求

- 浏览已发布文章列表
- 查看文章详情
- 按一级分类或二级分类查看文章
- 查看文章作者公开资料卡，仅展示用户名、昵称、头像和个人简介等基础信息

#### 8.4.2 后续扩展

- 评论
- 点赞
- 收藏

### 8.5 评论

#### 8.5.1 功能需求

- 登录用户可在允许评论的已发布文章下发表评论
- 登录用户可回复已经审核通过的评论
- 评论作者可删除自己发表的评论，但不可编辑评论内容
- 评论作者可查看自己的待审核和已拒绝评论及对应状态
- 游客和其他普通用户只能查看审核通过的评论

#### 8.5.2 展示规则

- 评论支持无限层级回复关系，数据层保留直接父评论和所属顶层评论
- 前端只展示两层：顶层评论单独展示，其下所有层级回复在第二层平铺
- 平铺回复时仍需展示直接被回复的用户，以保留对话上下文
- 顶层评论采用滚动加载，默认按创建时间倒序排列
- 同一顶层评论下的回复默认按创建时间正序排列
- 顶层评论和回复均只向非作者用户展示 `APPROVED` 状态的数据
- 顶层评论默认只展示可见回复总数，不预载回复内容；用户点击“共 x 条回复，点击查看”后再展开回复区域
- 首次展开加载 5 条回复，后续每次加载 10 条，直至展示完全部可见回复；回复区域支持收起

#### 8.5.3 审核与删除规则

- 普通用户新发表评论或回复默认状态为 `PENDING`，审核通过前仅评论作者和管理员可见
- 管理员新发表评论或回复直接创建为 `APPROVED`，无需审核，且不写入 `reviewedBy`、`reviewedAt`
- 站长负责评论审核，审核通过后状态变为 `APPROVED` 并公开展示
- 已拒绝评论仅评论作者和管理员可见，不计入公开评论数
- 用户删除自己的评论时，其所有层级子回复一并逻辑删除
- 管理员可以拒绝、隐藏或删除评论；隐藏和删除后的评论不再公开展示
- `comment_count` 统计文章下全部 `APPROVED` 评论，包括顶层评论和回复，不统计 `PENDING`、`REJECTED`、`HIDDEN`、`DELETED`

#### 8.5.4 内容与频率规则

- P1 评论仅支持纯文本，长度为 1-1000 个字符，去除首尾空白后不得为空
- P1 不解析评论中的 HTML 或 Markdown，前端必须按纯文本安全展示
- P2 再评估自定义表情、图片回复和受限 Markdown/代码内容
- 同一登录用户对同一文章每 10 秒最多发表一条评论或回复
- P1 使用 Redis 原子限流，限流维度为 `userId + articleId`，不依赖浏览器签名
- 被禁用用户不能发表评论或回复
- 文章 `allow_comment = false` 时禁止新增评论和回复，但已有审核通过的评论继续展示，评论作者仍可删除自己的已有评论

## 9. 游客视角 PRD

### 9.1 浏览能力

- 访问首页
- 浏览已发布文章列表
- 查看文章详情
- 按一级分类或二级分类浏览文章
- 查看置顶文章优先展示
- 查看文章作者公开资料卡
- 查看文章下审核通过的评论

### 9.2 权限限制

- 不可访问个人中心
- 不可访问后台
- 不可使用登录后能力
- 不可发表评论或回复

### 9.3 转化入口

- 可访问登录页
- 可访问注册页
- 在访问受限能力时，引导进行登录

## 10. 页面清单

### 10.1 P0 前台页面

- 首页
- 文章列表页
- 文章详情页
- 分类页
- 登录页
- 注册页
- 个人中心页
- 404 页

### 10.2 P0 后台页面

- 后台登录页
- 用户管理页
- 文章列表页
- 文章新建页
- 文章编辑页
- 分类管理页
- 标签管理页

### 10.3 P1 页面

- 关于我页面
- 项目作品页面
- 留言页面
- 后台评论管理页

## 11. 核心业务流程

### 11.1 注册流程

1. 用户进入注册页
2. 输入用户名、邮箱、密码
3. 前端进行基础校验
4. 后端校验用户名和邮箱唯一性
5. 注册成功后跳转登录页

### 11.2 登录流程

1. 用户输入用户名或邮箱，以及密码
2. 后端校验账号状态与密码正确性
3. 登录成功后签发 Access Token 和 Refresh Token
4. P0 阶段 Refresh Token 写入数据库会话表，并通过 `Set-Cookie` 写入 `refresh_token` HttpOnly Cookie；P1 阶段可同步写入 Redis 运行态会话
5. 前端保存 Access Token，使用 Access Token 访问受保护接口

### 11.3 刷新登录态流程

1. Access Token 过期
2. 前端请求刷新登录态接口，浏览器自动携带 `refresh_token` Cookie
3. 后端校验 Refresh Token 是否存在、是否过期、用户是否被禁用
4. 校验通过则签发新的 Access Token 和新的 Refresh Token，并通过 `Set-Cookie` 轮转 `refresh_token` Cookie
5. 后端将本次请求携带的旧 Refresh Token 标记为失效
6. 若刷新失败，P0 阶段统一要求前端清理本地登录态并重新登录；P1 阶段可结合 Redis 宽限期处理幂等重试

### 11.4 退出登录流程

1. 用户发起退出登录
2. 浏览器自动携带 `refresh_token` Cookie，后端尝试使当前 Refresh Token 失效；无需先刷新 Access Token，若 Refresh Token 缺失、已过期、已撤销或找不到对应会话，仍返回退出成功
3. 前端清理本地登录态
4. 后端通过 `Set-Cookie` 清除 `refresh_token` Cookie
5. 跳转到首页或登录页

### 11.5 文章发布流程

1. 管理员进入文章新建页
2. 输入标题、摘要、正文、分类、标签等内容
3. 可先保存为草稿
4. 确认内容后发布文章
5. 发布后的文章出现在前台列表
6. 如需撤回展示，可将文章下线

### 11.6 用户禁用流程

1. 管理员在后台禁用指定用户
2. 被禁用用户无法再次登录
3. 后端撤销该用户全部活跃 Refresh Token
4. P0 阶段该用户已有 Access Token 最长保留到 15 分钟短有效期自然过期
5. P1 阶段通过 Redis tokenVersion 校验，使禁用、改密、角色变更后的旧 Access Token 立即失效

## 12. 非功能性要求

### 12.1 安全性

- 密码必须加密存储，不可明文保存
- 接口需进行权限校验
- 后台接口需校验管理员角色
- Token 需支持失效控制

### 12.2 可扩展性

- 数据结构需支持评论、点赞、收藏、留言、项目、友链等后续模块扩展
- 接口设计尽量保留版本演进空间

### 12.3 易维护性

- 模块边界清晰
- 角色与权限定义明确
- 页面与接口命名规范统一

### 12.4 部署与运维

- 生产环境需支持容器化部署、数据库定期备份、对象存储和基础监控告警
- 具体部署与运维约定见 [deployment-ops.md](./deployment-ops.md)

## 13. 数据模型预留原则

### 13.1 总体原则

- P0 未实现的互动能力，不要求前端页面落地，但数据库需预留字段或表结构
- 统计型字段可以在主表冗余，行为明细应使用独立关系表
- 与认证相关的数据建议独立表设计，避免用户表职责过重

### 13.2 用户表预留

建议字段：

- `id`
- `username`
- `nickname`
- `email`
- `password_hash`
- `role`
- `status`
- `avatar_url`
- `bio`
- `last_login_at`
- `created_at`
- `updated_at`

为后续版本预留：

- `email_verified`
- `email_verified_at`
- `deleted_at`

### 13.3 认证会话表预留

建议设计独立的 `refresh_token` 或 `auth_session` 表。

建议字段：

- `id`
- `user_id`
- `token_jti`
- `token_hash`
- `expires_at`
- `revoked_at`
- `created_at`

为后续版本预留：

- `ip`
- `user_agent`
- `device_info`

### 13.4 文章表预留

建议字段：

- `id`
- `title`
- `summary`
- `content_md`
- `content_html`
- `content_text`
- `cover_url`
- `status`
- `category_id`
- `author_id`
- `is_top`
- `published_at`
- `created_at`
- `updated_at`

说明：

- `category_id` 存储文章绑定的二级分类 ID
- `content_md` 用于后台编辑与版本维护
- `content_html` 由 `content_md` 转译得到，用于前台渲染
- `content_text` 由 `content_md` 提取纯文本得到，用于全文搜索
- P0 文章详情 URL 使用文章 ID，例如 `/articles/40001`；`slug` 仅作为 P2 URL 可读化与 SEO 优化预留

为后续版本预留：

- `slug`
- `view_count`
- `comment_count`
- `like_count`
- `favorite_count`
- `allow_comment`

### 13.5 分类表预留

建议字段：

- `id`
- `name`
- `parent_id`
- `level`
- `description`
- `sort`
- `status`

为后续版本预留：

- `path`
- `seo_title`
- `seo_description`

### 13.6 标签表与关联表预留

标签表建议字段：

- `id`
- `name`
- `status`
- `description`

文章标签关联表建议字段：

- `article_id`
- `tag_id`

### 13.7 评论表预留

P0 不启用，但建议预留表结构。

建议字段：

- `id`
- `article_id`
- `user_id`
- `content`
- `status`
- `parent_id`
- `created_at`

### 13.8 点赞表预留

P0 不启用，但建议预留表结构。

建议字段：

- `id`
- `article_id`
- `user_id`
- `created_at`

### 13.9 收藏表预留

P0 不启用，但建议预留表结构。

建议字段：

- `id`
- `article_id`
- `user_id`
- `created_at`

### 13.10 其他模块预留

- `message_board`：用于留言模块
- `project`：用于项目作品模块
- `friend_link`：用于友链模块

## 14. P0 非目标范围

- 普通用户发文
- 评论功能
- 点赞功能
- 收藏功能
- 友链管理
- SMTP 邮箱验证
- 图片上传服务
- 多管理员协作
- 社区化能力
- 商业化能力

## 15. P0 验收标准

- 游客可以浏览已发布文章与分类内容
- 用户可以完成注册、登录、退出和刷新登录态
- 管理员可以完成用户管理、文章管理、分类管理、标签管理
- 文章支持草稿、已发布、已下线三种状态
- 前台仅展示已发布文章
- 标签不出现在主导航中
- 被禁用用户不能登录，已有会话在刷新时失效
- 数据结构已为评论、点赞、收藏、邮箱验证等后续能力预留扩展空间

## 16. 后续建议

- 基于本 PRD 继续输出数据库表设计文档
- 基于本 PRD 继续输出后端接口清单
- 基于本 PRD 继续输出前后台页面原型清单
