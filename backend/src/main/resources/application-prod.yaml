server:
  forward-headers-strategy: framework   # 处理 nginx 反向代理的配置

blog:
  auth:
    refresh-cookie:
      secure: true
  mail:
    enabled: ${BLOG_MAIL_ENABLED:true}
    frontend-base-url: ${BLOG_MAIL_FRONTEND_BASE_URL:}
